def login():
    usernameInput = input("Username : ")
    passwordInput = input("Password : ")
    if usernameInput == "admin" and passwordInput == "1234":
        return True
    else:
        return False


def showMenu():
    print("----- iShop -----")
    print("1. Vat Calculator (คำนวณราคาสินค้าชิ้นเดียว + VAT)")
    print("2. Price Calculator (รวมราคาสินค้า 2 ชิ้น + VAT)")


def menuSelect():
    userSelected = int(input("Selected Menu No. >> "))
    return userSelected


def vatCalculator(totalPrice):
    vat = 7
    result = totalPrice + (totalPrice * vat / 100)
    return result


def priceCalculator():
    price1 = int(input("First Product Price : "))
    price2 = int(input("Second Product Price : "))
    return vatCalculator(price1 + price2)

if login():
    print("Login Success!")
    showMenu()

    choice = menuSelect()

    if choice == 1:
        product_price = int(input("Product Price : "))
        print("Total Price (Included VAT 7%) :", vatCalculator(product_price))

    elif choice == 2:
        print("Total Price (Included VAT 7%) :", priceCalculator())

    else:
        print("Invalid Menu Selection.")
else:
    print("Login Failed! Incorrect Username or Password.")
